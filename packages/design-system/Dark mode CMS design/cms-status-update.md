## 2026-07-22 — Design-system session: CMS cleanup, Anthony task tracker, Page Editor prototype + spec, client onboarding docs

Paste into `da-platform/STATUS.md` above the most recent entry.

- **Design-system cleanup** — removed a stale bundled export (`Digital Allies CMS.html`) that duplicated `cms/dashboard.html`.
- **New: `cms/anthony-tasks.html`** — visual check-off tracker of every open `[Anthony]`-only item (Supabase/Vercel/registrar clicks), grouped by urgency, seeded from `STATUS.md` + `BUILD-SCHEDULE.md`. Checkbox state persists per-device via localStorage. Linked from `cms/index.html` + `cms/README.md`. Keep in sync going forward.
- **New: `cms/page-editor.html`** — clickable hi-fi prototype of the Pages block/section builder (the least production-ready admin area per Anthony's own Vercel Toolbar comments, "this build isn't meant for production"). Covers section stack with reorder/duplicate/delete, inline content editor, a section-library modal, a multi-tenant live-preview switcher (design-review convenience only, not real architecture), and a Starter/Pro/Agency gate on the code editor. Design reference only — not wired to real data.
- **New: `cms/PAGE_EDITOR_SPEC.md`** — the Next.js implementation notes: Page/Section data model, a `SECTION_REGISTRY` pattern (one entry per section type = schema + AdminForm + PublicBlock so admin forms and the public renderer never drift), API surface, and where section layouts should come from (DS-native components restyled per-client via `--tok-*`). Flags an open decision for Anthony: iframe vs. shadow-DOM sandboxing for Agency-tier custom section code.
- **New: `client-docs/welcome-letter.html`, `client-docs/setup-instructions.html`, `client-docs/platform-access-guide.html`** — printable, brand-voiced client onboarding documents (doc-page based, DA tokens/fonts) covering pre-launch onboarding and post-launch access. Static for now; wiring to real client records is a later phase, not scoped yet.
- **Module naming decision:** the admin backend is one shared build for every client, so module names must be generic (Services / Testimonials / Blog / Articles / Contact / Settings), not Digital Allies' own jargon (The Departments / Field Notes / The Press Office / Command Center). DA's vocabulary is reserved for DA's own admin instance only, via a per-tenant label map over the same registry/nav data — not a second codebase. Real, unbuilt code work.
- **Page-template picker scoped:** new pages start from Blank / Home / Blog Post / Case Study (generic, every client) or, DA-tenant only, Service/Tool Detail + Services Index — built FROM this design system's existing `ToolDetail.dc.html` / `ServicesIndex.dc.html`, not re-derived. Other clients get their own equivalents once their own design systems exist.
- **Reconciliation pass against `ARCHITECTURE.md` / `PIPELINE.md` / `THEME_ENGINE_PLAN.md` / `TODO.md`** — corrections now in `cms/PAGE_EDITOR_SPEC.md`'s "Reconciliation" section: `pages.blocks` is already one `jsonb` array (no Section-row migration needed); `BlockRenderer.tsx` block-type keys are already generic (only `AdminNav.tsx` labels carry DA jargon — the per-tenant label map above is real, unbuilt work); the prototype's multi-tenant live-preview switcher is a design-review convenience only (real architecture is one deployment = one client, no runtime tenant switching); Blog Post / Case Study page-template entries conflict with the existing `posts` table + Tiptap editor and were removed from the page-shell picker's real-build scope; the Starter/Pro/Agency code-editor gate should read the existing (unapplied) `clients.plan` migration rather than add a second field; the global token/theme editor question is already fully scoped in `THEME_ENGINE_PLAN.md`, no overlap.
- **Not done this session, explicitly deferred:** rebuilding `CMS_IMPLEMENTATION_PLAN.html`'s generic Node/Express/Mongo stack section to match the real Next.js/Supabase architecture — still inaccurate, flagged for a follow-up pass.
- **Open decision, not resolved:** whether `digitalallies.net` gets rebuilt on-platform (real Pages/Press Office/Settings, matching the "Connected" tier) vs. staying static with targeted patches. Recommendation is rebuild (per decision #2's "one platform, three faces" model), sequenced AFTER the Pages/section-builder work ships against real data.

**Verified:** reconciled line-by-line against `ARCHITECTURE.md`, `PIPELINE.md`, `THEME_ENGINE_PLAN.md`, and `TODO.md` — corrections captured in `cms/PAGE_EDITOR_SPEC.md`'s "Reconciliation" section. Design-only session, no application code touched.

**Placement:** `cms/` → `da-platform/packages/design-system/cms/` (existing mirror location). `client-docs/` → new `da-platform/packages/design-system/client-docs/` (same mirroring pattern; moves under `tools/build-workflows` once wired to real client records).
