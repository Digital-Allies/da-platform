Paste into `da-platform/AGENTS.md`'s Rules section (or a `packages/design-system/AGENTS.md` if that gets split out):

- `packages/design-system/cms/` mirrors design-review prototypes for the CMS admin (page editor, Anthony's task tracker) — reference/spec only, not the real Next.js build in `tools/build-workflows`. Treat `cms/PAGE_EDITOR_SPEC.md` as the implementation spec once this work is picked up for real; its "Reconciliation" section already corrects it against `ARCHITECTURE.md` / `PIPELINE.md`.
- `packages/design-system/client-docs/` — printable, brand-voiced client onboarding documents (welcome letter, setup instructions, post-launch access guide). Static for now; wiring to real client records is a later phase.
- **Module naming: generic by default, DA jargon is an override, not the default.** The admin backend is one shared build for every client — nav/module labels must read generically (Services, Testimonials, Blog, Contact, Settings) for every tenant except Digital Allies' own instance, which gets its proprietary vocabulary (The Departments, Field Notes, The Press Office, Command Center) via a per-tenant label map over the same registry/nav data. Never fork the codebase to get DA's naming.
- **Page-shell templates:** Blank / Home / Blog Post / Case Study are generic, every client. Service/Tool Detail + Services Index are DA-tenant only — build FROM `ToolDetail.dc.html` / `ServicesIndex.dc.html` in `packages/design-system`, don't re-derive.
