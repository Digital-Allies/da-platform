import OnboardingClient from './OnboardingClient';

export const metadata = {
  title: 'Onboarding Guide | Digital Allies CMS',
};

export default function OnboardingPage() {
  return <OnboardingClient />;
}
